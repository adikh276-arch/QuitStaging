server {
    listen 80;
    server_name localhost;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 10240;
    gzip_proxied expired no-cache no-store private auth;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml application/javascript;
    gzip_disable "MSIE [1-6]\.";

    # Redirect root to /quit_staging/
    location = / {
        return 301 /quit_staging/;
    }

    # Handle /quit_staging (no slash) by redirecting to /quit_staging/
    location = /quit_staging {
        return 301 /quit_staging/;
    }

    location = /favicon.ico {
        alias /usr/share/nginx/html/quit_staging/favicon.ico;
    }

    # Main application location
    location /quit_staging/ {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /quit_staging/index.html;

        # Mitigate QUIC/HTTP3 issues
        add_header Alt-Svc 'clear';
        
        # Security headers
        add_header X-Content-Type-Options "nosniff";
        add_header Content-Security-Policy "frame-ancestors 'self' https://*.mantracare.com;";

        # Asset caching
        location ~* \.(?:css|js|map|json|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|otf)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
            add_header Alt-Svc 'clear';
            access_log off;
        }
    }

    # Error handling - ensure 404s serve the SPA index
    error_page 404 /quit_staging/index.html;
}
